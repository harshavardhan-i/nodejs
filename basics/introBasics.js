// Node.js is a backend technology.
// Node allows us to create backend using JS.
// It is scalable and really fast.

// Node JS allows javascript to run outside of browsers.
// With node JS we can interact directly with the hardware of computer/servers.
// Helps build apps like atom editor.

// Below command helps check version of node.
// node --version 

// Run JS in cmd - node <fileName>.js

// Node REPL - Read Evaluation Print Loop
// Allows to execute code in byte size chunks
// Lets you run JS code in terminal just like browser console
// node : command to enter REPL
// .exit : command to exit REPL, alternatively use Ctrl+C twice.

// Node comes with pre-built Modules(bunch of libraries)

// Steps to setup node.
// 1. Create new directory and add new JS file
// 2. cmd: npm init - Creates package.json
//      cmd: npm init -y - Creates all and says yes to all. 
// 3. Install external modules if required - Create package-lock.json & node_modules.